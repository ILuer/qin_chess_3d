# 秦风 · 3D 中国象棋 —— 交付总览

> 纯前端 3D 中国象棋。零构建、零后端、零外部二进制资源，浏览器直接运行。
> 交付日期：2026-08-05 ｜ 质量门判定：**PASS（附 CONCERNS）**

---

## 一、怎么跑起来

```bash
cd qin-chess-3d
node serve.mjs          # 默认 5173 端口，可传参：node serve.mjs 8080
# 浏览器打开 http://localhost:5173
```

> **必须走 HTTP，不能 `file://` 双击打开。**
> 原因：AI 使用 module Web Worker，`file://` 协议下会被浏览器的同源策略拒绝。
> `serve.mjs` 是零依赖静态服务器（Node 原生 http 模块，76 行），不需要 `npm install`。

唯一的外部依赖是 Three.js 0.169.0，通过 `index.html` 里的 importmap 从 unpkg CDN 加载 —— 首次运行需要联网。

---

## 二、项目结构

```
qin-chess-3d/
├── index.html                  # importmap + DOM 骨架 + 内联兜底 CSS
├── serve.mjs                   # 零依赖静态服务器
├── README.md                   # 使用说明
├── overview.md                 # 本文件
├── styles/
│   └── main.css                # 秦风暗色 UI 皮肤（597 行）
├── src/
│   ├── main.js                 # 启动编排：分阶段加载 → 开局动画 → 主循环（584 行）
│   ├── core/                   # ── 纯逻辑层，零 Three.js 依赖 ──
│   │   ├── constants.js        #    坐标契约、棋子规格、名称表、配色、时序（306 行）
│   │   ├── board.js            #    纯数据棋盘：get/set/clone/findKing/toFen（220 行）
│   │   ├── rules.js            #    规则引擎：合法走法、将军、终局判定（527 行）
│   │   └── gameState.js        #    回合管理、历史栈、中文记谱、胜负判定（389 行）
│   ├── render/                 # ── 渲染层 ──
│   │   ├── scene.js            #    场景/相机/渲染器/OrbitControls/灯光/视角预设（387 行）
│   │   ├── materials.js        #    材质库 + 程序化贴图（雷纹/木纹/文字）（735 行）
│   │   ├── pieceFactory.js     #    7 种棋子的程序化 3D 建模（694 行）
│   │   ├── boardMesh.js        #    棋盘 + 环境（火盆/旗帜/地台）（392 行）
│   │   ├── animator.js         #    补间引擎：弧线移动/消融/挤压/震屏（446 行）
│   │   └── effects.js          #    选中环/落点提示/被将脉冲/粒子（675 行）
│   ├── ui/
│   │   ├── input.js            #    Raycaster 拾取、点选走子、悬停反馈（344 行）
│   │   ├── hud.js              #    回合指示/将军横幅/棋谱/吃子区/Toast（418 行）
│   │   └── controls.js         #    按钮 + 键盘快捷键 + 确认弹窗（237 行）
│   ├── ai/
│   │   ├── engine.js           #    Worker 优先 + 主线程时间切片兜底（190 行）
│   │   ├── search.js           #    Minimax + αβ + 迭代加深 + 静态搜索 + PST（413 行）
│   │   └── worker.js           #    纯逻辑 module worker（30 行）
│   └── audio/
│       └── sfx.js              #    WebAudio 程序化音效合成，10 种音（1099 行）
├── docs/
│   ├── CONTRACT.md             # 跨模块接口契约（并行开发的唯一真相源）
│   ├── gdd.md                  # 游戏设计文档
│   ├── ux-spec.md              # 交互规格
│   ├── juice-oil-windex.md     # 体验设计核心规格
│   ├── art-bible.md            # 美术圣经
│   ├── audio-design.md         # 音频设计
│   └── qa-report.md            # 质量门报告 + D1–D12 人工验证清单
└── tests/
    ├── rules.test.mjs          # 规则引擎 95 条断言
    ├── integration-check.mjs   # 导入导出一致性核对
    ├── syntax-check.mjs        # ESM 语法检查
    └── cleanup-temp.mjs        # 临时文件清理（带白名单保护 + --dry 预演）
```

总计约 **6000 行**源码 + 约 1900 行文档。

---

## 三、需求对照

| # | 需求 | 实现 | 落点 |
|---|------|------|------|
| 1 | 9×10 棋盘，河界/九宫清晰 | 河界为下沉水面槽 + "楚河汉界"篆书刻字；九宫为斜线阴刻 + 地面微光 | `boardMesh.js` |
| 1 | 7 种棋子独立 3D 建模 | 全部程序化几何体建模，**无一是平面圆片贴字** | `pieceFactory.js` |
| 2 | 移动平滑过渡 | 抛物线弧线移动 + 落子挤压回弹 + 棋盘冲击波 | `animator.js` |
| 2 | 吃子攻击 + 消失动画 | 攻击方前冲（lunge）→ 被吃方消融粒子化 → 震屏 | `animator.js` / `effects.js` |
| 2 | 选中高亮 + 落点提示 | 旋转光环 + 落点半透明标记（可走/可吃视觉分级）+ 受阻灰叉 | `effects.js` |
| 3 | 完整规则校验 | 见下表 | `rules.js` |
| 4 | 鼠标点选 + 视角旋转缩放 | 单击选中/落子，OrbitControls 旋转缩放（禁用平移防迷失） | `input.js` / `scene.js` |
| 4 | 重开/悔棋/棋谱/回合/将军 | 全部实现 + 吃子陈列区 + Toast + 帮助面板 | `hud.js` / `controls.js` |
| 5 | 流畅帧率 + 加载提示 | 几何体合并（28–45 部件 → 8–18 Mesh）、像素比钳制 ≤2、分阶段加载进度条 | `main.js` / `pieceFactory.js` |
| 6 | Juice / Oil / Windex | 独立规格文档 + 全局仲裁优先级 | `docs/juice-oil-windex.md` |

**规则覆盖**（`tests/rules.test.mjs` 95 条断言全通过）：

| 规则 | 状态 |
|------|------|
| 将/帅 限九宫、士 斜行九宫 | ✓ |
| 象 田字 + **塞象眼** + 不过河 | ✓ |
| 马 日字 + **蹩马腿** | ✓ |
| 车 直线、炮 直行 + **翻山吃子** | ✓ |
| 兵/卒 过河前只进、过河后可平 | ✓ |
| **白脸将**（将帅照面）判非法 | ✓ |
| 将军 / 将死 / 困毙（困毙判负，符合中国象棋规则） | ✓ |
| 自杀走法（走后自己被将）拒绝 | ✓ |
| 中文纵线记谱（红中文数字、黑阿拉伯数字、前/后消歧） | ✓ 固化断言 |

---

## 四、7 种棋子的形象设计

全部用 Three.js 原生几何体程序化生成，**不加载任何模型文件**。秦制玄黑 + 赤红配色，红黑双方靠**四重冗余**区分（不只靠颜色）：明度差 ΔL\*≥25、圆形/八角形底座、阳刻/阴刻字形、不同汉字。

| 棋子 | 形象 | 关键结构 |
|------|------|----------|
| 兵/卒 | 秦军步兵 | 圆筒身甲 + 板甲片 + 头巾髻 + 斜持长戈 |
| 马 | 骑兵 | 马体 + 马首 + 四蹄 + 背上骑手 + 竖立骑枪 |
| 象/相 | 文士 | 高冠 + 宽袖深衣 + 手捧竹简 |
| 士/仕 | 卫兵 | 方形头盔 + 环首刀 + 立盾 |
| 车 | 战车 | 车厢 + 双辐条轮 + 车辕 + 车轼 + 竖旗 |
| 炮 | 抛石机 | 三角支架 + 长抛杆 + 石弹兜 + 配重箱 |
| 将/帅 | 主帅 | 最高体量 + 冕旒冠 + 大氅 + 佩剑 + 帅旗 |

（顶部高度 `PIECE_TOP_Y`：兵 0.79 → 马 0.95 → 车 0.92 → 炮 1.00 → 帅 1.05，体量层级即棋力层级 —— 这是一条 Windex 线索：不看字也能大致读出棋子重要性。）

---

## 五、体验设计三原则的落地

设计策略上确立了**仲裁优先级：Oil > Windex > Juice** —— 当三者冲突时，先保证不添堵，再保证看得懂，最后才追求爽感。

**Oil（润滑油，减少摩擦）**
- 输入锁 420ms 与视觉动画 700ms **解耦** —— 动画还在演，你已经可以下一步了
- 输入缓冲（长度 1）—— 手快点了下一步不会丢
- 切换选中不需要先取消，直接点另一个子
- 走了非法步不会清空选中，你不用重新点一遍
- 点击 vs 拖拽阈值 5px，拖动视角不会误触发选子

**Windex（玻璃清洁剂，规则透明）**
- 默认相机仰角 55–60°（实测 phi=0.64 → 53.3°）—— 3D 观感和棋盘可读性的平衡点；空格键一键切正俯视
- 非法走法给**具体原因**而非笼统报错，26 条原因文案，按 形→域→路→局 优先级链输出（例："马腿被堵"而不是"不能这么走"）
- 落点提示区分「可走」与「可吃」两种视觉；受阻点显示灰叉
- 被将时国王持续泛红 + 全屏红色脉冲 + 横幅

**Juice（果汁，正反馈）**
- 弧线抛物线移动、落子挤压回弹、棋盘冲击波
- 吃子：攻击方前冲 → 被吃方消融粒子化 → 震屏
- 青铜钟音效用非谐分音列 [1, 1.19, 2.0, 2.76, 3.61, 5.42] 合成，带 1.5 秒程序化混响 IR
- 10 种音效全部 WebAudio 实时合成，零音频文件

---

## 六、关键工程决策

1. **零构建、零 npm** —— importmap 直接从 CDN 加载 Three.js。代价是首次运行需联网，收益是双击 `serve.mjs` 就能跑，没有 node_modules 黑洞。
2. **契约先行的并行开发** —— 先写 `docs/CONTRACT.md`（坐标系、尺寸、模块导出签名、文件归属边界），4 个方向并行开发，最终 112 个跨模块符号**零断链**。
3. **逻辑层零渲染依赖** —— `src/core/` 不 import Three.js，因此可以在 Node 里直接跑 95 条规则断言，也可以直接塞进 Web Worker 给 AI 用。
4. **AI 双通道** —— 优先 module Web Worker（不卡 UI），失败则降级到主线程时间切片。难度 1/2/3 = 深度 2/3/4，时限 400/900/1800ms。
5. **程序化一切** —— 模型、贴图、音效全部代码生成，项目里没有一个二进制资源文件。

---

## 七、质量门结果

| 检查项 | 结果 |
|--------|------|
| 规则引擎测试 `tests/rules.test.mjs` | **95 通过 / 0 失败** |
| 导入导出一致性 `tests/integration-check.mjs` | **18 文件 / 43 import / 112 符号 / 0 断链** |
| ESM 语法检查 `tests/syntax-check.mjs` | **21 / 21 通过** |
| 静态资源可达性（HTTP 200） | index.html、main.js、main.css、pieceFactory.js、sfx.js、worker.js 全部 200 |
| 性能抽查 | 2000 次全量合法走法生成 131ms（共 88000 步） |
| 外部二进制资源 | **0 个**（已核验） |

**综合判定：PASS（附 CONCERNS）**

---

## 八、已知问题与后续建议

**CONCERNS（不阻塞运行，建议下一轮收敛）**

| 编号 | 问题 | 影响 | 建议 |
|------|------|------|------|
| G1 | `scene.js` 自定义了灯光，未采用 `boardMesh.js` 导出的 `LIGHT_PRESET` | 材质是按美术预设调的，当前白色环境光 @0.16 可能让阴影偏平、金属质感减弱 | 切换到 `LIGHT_PRESET`（冷蓝环境光 0x2b3444 @0.55）后重新对比 |
| G2 | `PALETTE` 在 `constants.js` 与 `materials.js` 各定义一份 | 实际是职责分离（引擎侧特效/灯光色 vs 美术侧材质色），非真重复，但违背契约原意 | 重命名为 `ENGINE_PALETTE` / `MATERIAL_PALETTE`，或加注释说明边界 |
| G3 | 悔棋 / 重开是瞬时重建棋子 | 缺少过渡，稍显生硬 | 加入回退补间动画 |
| L1 | 棋谱点击回溯目前是预览，非真实回退 | 功能弱于预期 | 接入历史栈实现真回溯 |
| L2 | 项目根残留 47 个校验临时文件（`_chk*`、`_check*` 等） | **不影响运行**（`index.html` 只加载 `src/main.js`） | 运行 `node tests/cleanup-temp.mjs`（先 `--dry` 预演） |

**待人工验证** —— `docs/qa-report.md` 里有 D1–D12 共 12 项浏览器动态交互清单（选中高亮、落点视觉分级、弧线动画、吃子消融、非法原因 Toast、拖拽 vs 点击、悔棋、快捷键、AI 响应、将军脉冲、音频、帧率）。这些需要在真实浏览器里点一遍才能确认，自动化测试覆盖不到。

---

## 九、快捷键

| 键 | 功能 |
|----|------|
| `R` | 重新开局 |
| `U` / `Ctrl+Z` | 悔棋 |
| `F` | 翻转视角（红方 ↔ 黑方） |
| `空格` | 切换正俯视 |
| `M` | 静音开关 |
| `?` | 帮助面板 |
| `Esc` | 取消当前选中 |
