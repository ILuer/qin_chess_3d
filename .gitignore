# ---- 依赖与构建产物 ----
node_modules/
dist/
dist-*/
build/
*.log

# ---- 会话内验证残留（沙箱禁删临时 txt，gitignore 防呆；由有权限者物理清理）----
# 仅忽略仓库根的下划线 txt（如 _probe_*.txt / _tc_out.txt 等调试重定向产物）；
# _headers 无扩展名不受影响，仍须入库（Cloudflare Pages 响应头）。
/_*.txt

# ---- TS 迁移（2026-08-20）：src 全面启用 TypeScript，js 仅物理残留待清理 ----
# 迁移完成：src/** 已 .ts 化；工作树残留 .js 因环境写保护无法物理删除，
# 通过 git rm --cached 移出索引 + 本规则防呆，禁止任何 js 重新入库。
src/**/*.js
npm-debug.log*

# ---- 测试工具链 ----
# 2026-08-17 用户拍板变更（部署模式 B：CF Pages 构建命令 npm ci && npm run build）：
# package.json / package-lock.json 由「暂不入库」改为「必须入库」——
# CF 构建环境靠它们安装 esbuild 并执行构建；tests/ 仍不入库（保持）。
tests/
coverage/
.nyc_output/

# ---- 编辑器 / 系统 ----
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db
desktop.ini

# ---- Cloudflare / Wrangler ----
.wrangler/
.dev.vars

# 注意：不要在此忽略下划线开头的文件。
# _headers 是 Cloudflare Pages 的响应头配置，必须入库。

# ---- 本地验证生成的临时副本（由 dev/transform_phase3.mjs 重新生成，不入库）----
_qa/p3out/animator_test.mjs
_qa/p3out/constants_test.mjs
_qa/p3out/sfx_test.mjs
_qa/p3out/animator.mjs
_qa/p3out/main.mjs
_qa/p3out/sfx.mjs
_qa/p3out/constants.mjs
_qa/p3out/pieceFactory.mjs
_qa/p3out/err.txt
.cache/

# ---- QA 冒烟产物（cdp-smoke / build / check-vendor 输出，2026-08-17 起整目录不入库）----
_qa/
